syntax = "proto3";

package chat;

option go_package = "github.com/WORUS/grpc_golang";

message Message {
    string body = 1;
}

service ChatService {
    rpc SayHello(Message) returns (Message) {}
}

